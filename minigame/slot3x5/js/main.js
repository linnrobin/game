// js/main.js
import { SlotMachine } from './slotMachine.js'; // Import the main game logic

// Initialize the game when the DOM is fully loaded
window.addEventListener('DOMContentLoaded', () => {
    SlotMachine.init();
});